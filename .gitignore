# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
# The built `vara` binary at the repo root. Anchored with a leading slash so it
# does NOT also match the cmd/vara/ source directory (a bare `vara` did, which
# silently excluded the CLI entry point from version control).
/vara

# Test binaries
*.test
*.out

# Coverage
coverage.out
coverage.html

# Profiling
*.prof
*.pprof
mem.out
cpu.out

# Go workspace
go.work
go.work.sum

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.bak

# Benchmark results
benchmark-results.txt
